# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

## What this is

A set of clickable HTML/CSS/JS prototypes for "Greenstreets" — no build system, no package.json, no server. Each prototype is a **single self-contained HTML file** you open directly in a browser (double-click or drag into a browser tab). There is no dev server, bundler, linter, or test suite in this repo — "running" a prototype just means opening the file.

Not a git repository — there is no version control here. Treat file edits as final; there's no diff/history safety net, so be extra careful with destructive changes and consider keeping backups (the `old/` folder is exactly that: prior iterations kept for reference).

## Shared stylesheet & script — `css/greenstreets-theme.css` + `js/greenstreets-theme.js`

The v6 glass design system now lives in **two external files linked by all active prototypes (01 v7 — current — plus the frozen 01 v6, 02, 03, 04)**: [css/greenstreets-theme.css](css/greenstreets-theme.css) (the look) and [js/greenstreets-theme.js](js/greenstreets-theme.js) (shared behaviour — the animated focus outline, the themed custom selects, the button-click ripple + particle burst, and the generic listing filter). **Current cache-busting version: `?v=22`.** The CSS is the single source of truth for the shared look — edit design tokens and shared component styles (`:root`, `.sidebar`, `.main`, `.grp`, `.btn-p`, `.fi`, `.tbl`, `.pill-*`, `.login-*`, etc.) **here**, not inline. Both files are linked with a `?v=N` cache-busting query in every page — **bump N whenever you change either file** (browsers aggressively cache them, and a stale cache once made a fix look broken).

- **01 (v6 admin)** has no inline `<style>` anymore — its `<head>` just links the shared file. Its theme-customizer JS still lives inline and drives the same CSS variables.
- **02 / 03 / 04** keep their own inline `<style>` (page-specific components like `.pkg-grid`, `.reminder-*`, `.pshell`, `.product-row-*`) and then link the shared file **after** the inline block, so the shared glass rules override the older flat-navy ones by source order. 04 also has a tiny inline override after the link (transparent `.pshell`, glass `.ph`) so the shared body gradient shows through.
- **Animated focus outline** (fields, selects & buttons): on focus/click the control's outline is *drawn* in the focus color — the stroke starts at the centre of the top & bottom edges and spreads symmetrically around the rounded corners to the left & right sides, completing a full rounded border. It's a **conic-gradient border ring** on a lightweight overlay element (`.fs-ring`) that **`js/greenstreets-theme.js`** creates and positions over whatever control is focused (one at a time). The ring is inserted as an **`position:absolute` sibling of the focused control** (not a fixed overlay on `<body>`) so it shares that control's stacking context — it hides behind a modal when the underlying field is behind one, shows above content for a field inside the modal, and scrolls with the control (so `.main`/`.pbody` are `position:relative`). An overlay is used — rather than the control itself — because a `<input>` can't take a `::before` and a mask on the input would hide its own text; the conic ring + `border-radius` is what removed the bare-corner gap the old 4-edge linear-gradient left. The reveal is driven by a **JS `requestAnimationFrame` tween** in `greenstreets-theme.js` that animates the `--fs-a` angle 0°→90° (with easing parsed from `--field-stroke-ease`) — a JS tween is used rather than a CSS `@keyframes`/`@property` animation because animating a registered custom property isn't supported on every browser and gets silently disabled by `prefers-reduced-motion` (that combination is exactly what once made the draw disappear). The mask (`mask-composite:exclude`) turns the conic disc into a border-only ring of `--field-stroke-weight`. Driven by `--field-stroke-color` (default the accent blue) / `--field-stroke-weight` (0 = off) / `--field-stroke-speed` / `--field-stroke-ease`, live-tunable in the theme customizer under the **"Field focus animation"** group (color, border width, duration, easing dropdown).
- **Themed custom select**: native `<select>` popups are OS-drawn and can't be given the glass look, so `greenstreets-theme.js` upgrades every `select.fi` into a `.cs-wrap` (a `.fi cs-trigger` div + a themed `.cs-menu`/`.cs-opt` dropdown), keeping the real hidden `<select>` for its value and dispatching a bubbling `change` on selection so existing `onchange` handlers (table filters etc.) still fire. Call `window.GSEnhanceSelects(root)` after injecting selects dynamically (the enhancer runs once on load over existing `select.fi`).
- **Theme customizer grouping**: `buildThemePanel()` renders labeled sections in a fixed order — Presets, Brand accent, Status colors, Background, Field focus animation, Screen entrance, Login card, Sidebar, Imagery & effects, **Button click effect**, Save & load — via small row builders (`tcColorRow`/`tcColorGroup`/`tcRangeRow`/`tcEaseRow`/`tcSpotlightColorRow`/`tcSidebarColorRow`/`tcFxColorRow`/`tcFxToggleRow`). The panel layout references controls by key, so `THEME_GROUPS`/`EXTRA_CONTROLS` order no longer dictates panel order; export/import/reset still iterate those arrays. Easing is a non-range/non-color control persisted via `state.fieldEase` in collect/apply and cleared in `resetTheme`; the ripple/particle colours + enable flags are likewise persisted separately (`state.rippleColorHex`/`particleColorHex`/`rippleEnabled`/`particleEnabled`).
- **Screen entrance cascade**: on every screen change, the content blocks ease in from the top-left (top→bottom, left→right) with a short per-block stagger (`@keyframes screenIn`). It targets the top-level children of the layout content container — `.main>*` (admin shell), `.login-wrap>*` (login), `.pbody>div>*` (supplier portal, which wraps its content in one div) — with `nth-child` delays. Driven by `--anim-dur` / `--anim-shift` / `--anim-stagger` / `--anim-ease` (all near the top of the CSS `:root`); duration, distance, and stagger are live-tunable in the theme customizer under the "Screen entrance" group. Because switching screens toggles `display:none↔flex`, the animation re-runs on each visit. It replaced the old whole-screen `rise` animation.
- **Button-click ripple + particle burst**: every button (`button, .btn-p, .btn-g, .btn-g-sm, .pnb, [role="button"]`) **and every clickable listing row/card** (`tr[onclick]`, `.product-row-card`) fires a ripple ring + an outward particle burst on **`pointerdown`** (so it shows the instant of press, even for buttons that navigate away on the following `click` — that timing was why the login button showed nothing when it fired on `click`). Keyboard activation (`click` with `detail===0`) bursts from the control's centre. Both are drawn on a single fixed overlay `#gs-fx-layer` (`z-index:9999`, created lazily by `greenstreets-theme.js`) so nothing clips them; elements self-remove on `animationend` (with a `setTimeout` fallback). Every knob is a `:root` var tuned live under the **"Button click effect"** customizer group: `--ripple-enabled` / `--ripple-color-hex`+`--ripple-rgb` / `--ripple-opacity` / `--ripple-duration` / `--ripple-ease` / `--ripple-size`, and `--particle-enabled` / `--particle-color-hex`+`--particle-rgb` / `--particle-opacity` / `--particle-count` / `--particle-size` / `--particle-distance` / `--particle-duration`. Per-particle fly offset is set inline as `--gs-px`/`--gs-py`.
- **Press feedback**: buttons **scale down slightly on `:active`** (`transform:scale(.955)`) rather than translating down — see the "PRESS FEEDBACK" block in the CSS. **Rows/cards deliberately do NOT get a press-scale**: `:active` also matches an element's ancestors, so a `transform` on a `<tr>`/card becomes a containing block for the absolutely-positioned focus ring and displaced it on mouse-up (the ring jumped up to the header). The click ripple is the row/card press feedback instead. The old `.btn-p:active{translateY(1px)}` was removed.
- **Reduced motion**: the `prefers-reduced-motion` gates that used to disable the screen-entrance cascade and the ripple/particles were **removed on purpose** — these are demo prototypes meant to *show* motion, and a designer machine with "reduce motion" on was silently killing them. (The card-glow login contour still respects it.) Re-add the gates behind a toggle if a production/accessible build is ever needed.
- **Generic listing filter** (`window.gsFilterToolbar(el)`): lets any table get a working search+filter bar with almost no per-table JS. Put a `.filter-toolbar` directly above a table containing a search `input` (`oninput="gsFilterToolbar(this)"`) and/or `select.fi` controls (`onchange="gsFilterToolbar(this)"`); rows are shown/hidden by plain-text match (a `<tr>` must contain every active term — select `value`s are lowercase substrings of the displayed cell text, `all`/`any` mean no filter). Scoped to the nearest `.grp-body` / `.landing-tab-panel` / `.grp`. **Convention: every multi-row data listing gets a filter bar** — this pass added them to the previously-bare tables (01: packaging components, EPR registrations, PPWR checks, packaging detail, proof docs, users sub-table; 02: current users, EPR registrations, quantity reports; 04: supporting documents). The intentionally-skipped ones are fixed contextual tables (02 `ra4_validate` import preview, 02 `ra10` DoC-request context).
- **Sidebar active nav indicator**: the active `.nav-item` accent bar sits on the **right** edge (`::before{right:-8px}`), coloured light green (`--gs-l`) with a glow, and the row background is a green gradient concentrated on the right (`linear-gradient(90deg,transparent,…,rgba(143,227,182,.24))`).
- **Listing / menu-option burst is gentler**: clicking a listing row/card or a dropdown option (`.cs-opt`, added to the burst set) fires a *reduced* ripple + particle spread scaled by `--ripple-row-scale` (default `.5`, tunable under "Button click effect" → "Listing ripple size"). The row ripple is sized from the row **height** (not the full row width) so it stays compact; number-stepper buttons (`.gs-num-steppers`) are excluded from bursts entirely.
- **Themed number stepper** (`window.GSEnhanceNumbers(root)`, run on load): native `<input type=number>` spinners are OS-drawn and off-theme, so they're hidden (`::-webkit-inner-spin-button`) and a themed ▲/▼ `.gs-num-steppers` control is injected into the field's positioned wrapper; a `.fi-unit` suffix is nudged left so the two don't collide. Stepper clicks call the input's step logic and dispatch `input`+`change`.
- **Sortable data-table headers** (`wireSortableTables()` on load; `window.gsSortTable(th)`): every `table.tbl` header becomes click-to-sort with a `.gs-sort-arrow` (▲ asc / ▼ desc, faint ↕ otherwise); numeric when the cell looks numeric, else alphabetical. Skips **action columns** — empty headers, ones labelled "Actions", or ones whose body cells contain a `<button>` — so they aren't sortable or a stray focus/outline target; also skips headers that already have an `onclick`, tables with `data-nosort`, and the `pt-table*` paginated tables that own their own sort engine.
- **Themed custom select — polish**: an open `.cs-wrap` is lifted to `z-index:1000` so its dropdown paints over later-in-DOM siblings (a wizard's Back/Next footer was covering it); highlighted `.cs-opt`s get a light contour + a fast center-outward highlight (`@keyframes csOptIn`); picking an option fires the small particle burst (see above).
- **AI review fields (04, `.air-feat`)**: each field carried BOTH an editable text input and a single-option placeholder `<select>`, which showed as a "doubled field" in edit mode — the select is now hidden for `.air-feat` in `.pkg-sec-edit-mode` (the text input is the real control). When the user edits a suggested value, `gsAirEdited` marks it user-verified: it hides the confidence badge, sets `data-conf="user"` (clearing the amber/red attention styling + left border), and un-dims a previously-"missing" value.
- **Data-grid toolkit** (`initListings()` on load; per-table work wrapped in try/catch so one odd table can't break the page): any `.tbl` listing that has a `.filter-toolbar` above it is upgraded into a real data grid. It skips the `pt-table*` paginated product tables (they own their sort/pagination engine via `ptInit`). Features:
  - **Grouped filter bar** — the toolbar is reflowed to `[search] · [clustered filter group `.ftb-group`] · [right-aligned tools `.ftb-actions`]`; `.filter-toolbar` is now flexbox (was grid). The search sits apart; the filter selects are visually grouped in a bordered cluster.
  - **Row tools** — a "Compact" density toggle (adds `.tbl-compact`), a "Columns" chooser (`.gs-colmenu` checkboxes that show/hide columns — also zeroes the matching `<col>` width on `colgroup` tables), and "Export" (CSV of the *current* filtered/sorted/visible view, honouring hidden columns).
  - **Bulk select** — if the table **already ships its own leading checkbox column** (e.g. 02's supplier tables), the toolkit *reuses* it (tags the cells `.gs-check-col`/`.gs-row-check` and wires them) instead of adding a second — otherwise it prepends one (`thead` + every row + `colgroup`). Plus a select-all with indeterminate state and a floating `.gs-bulkbar` ("N selected · Export selected · Clear") when ≥1 row is checked. Checkbox cells `stopPropagation` so they don't trigger row navigation/burst. Row visibility for counts is read from `.gs-filtered`/`.gs-paged`, not inline `display`.
  - **Pagination + count** — every enhanced listing gets a bottom `.gs-pager` (count on the left "Showing A–B of N", controls on the right: Rows-per-page select, Prev / Page X of Y / Next, and a "Go to" page-jump select), so it scales to hundreds of rows; with few rows it's a single page with the controls disabled. `gsRenderPage` shows only the current page's slice of the matched rows. **Filtering marks rows with `.gs-filtered` and pagination with `.gs-paged`** (both `display:none` via CSS) — the toolkit no longer sets row `display` directly. A redundant pre-existing static "Showing X of Y" `.tbl-footer` is auto-hidden (the pager replaces it).
  - **Empty state** — when a filter matches nothing, an in-table `.gs-empty-row` ("No matching rows · Clear filters") appears (`gsEmptyState`/`gsClearFilters`).
  - **Search highlight** — `gsHighlight` wraps matches in `<mark class="gs-hl">` in **text nodes only** (never mutates element markup, so pills/badges survive); ≥2 chars, visible rows only.
  - **Sticky header + scroll** — `.tbl thead th` is `position:sticky` with an opaque backing; lists with >12 rows get their `.tbl-wrap` capped to `max-height:60vh; overflow:auto` so the body scrolls under the header.
  - **Keyboard nav** — tables are focusable; ↑/↓ move a `.gs-kbd` highlight, Enter activates the row (`row.click()`).
  - `gsAfterFilter(table, term)` is the single refresh entry point (count + highlight + empty + bulk state) and is exposed on `window`. Sorting is tri-state (asc→desc→cleared, remembering insertion order in `table._gsOrig`) with **shift-click** for secondary/tertiary keys (numbered arrows).
  - **Keyboard nav** uses roving focus: the table is one Tab stop (`tabIndex=0`); tabbing onto it moves real focus to the first/highlighted visible row (`tabindex=-1` rows), ↑/↓/Home/End rove, Enter activates (`row.click()`). Sortable headers are keyboard-reachable (`tabindex=0` + `role=button`, Enter/Space to sort).
  - **Pre-existing decorative toolbars**: `gsWireToolbar` wires only the **search** input to `gsFilterToolbar` (a search matches any cell text); it deliberately does NOT auto-wire pre-existing `<select>`s, whose labels ("EU only") may not appear verbatim in cells and would otherwise hide every row. Purpose-built selects keep their own `onchange`.
- **Focus-outline positioning** (`place()`): the `.fs-ring` is positioned via `getBoundingClientRect` relative to the ring's own `offsetParent` (with `clientLeft/scrollLeft` correction), NOT `curEl.offsetLeft` — inside a `<table>` the control's `offsetParent` is the cell/table while the ring's containing block is `.main`/`.pbody`, and the mismatch used to throw the outline far to the left for buttons in table cells.
- **Image paths**: shared CSS lives in `css/`, so background images must be referenced `../img/…` (lowercase `img/`) — `url('Img/…')` resolved to the non-existent `css/Img/…` and silently 404'd (that's why the login background + swoosh texture were blank). Assets: `img/BackgroundGreenStreets.jpg`, `img/swoosh.png`, `img/icons-<page>.svg` (icon-sprite reference copies). **Icon SVG deduplication**: repeated inline icons in each page's *static HTML* were collapsed into an **inline `<symbol>` sprite** injected right after `<body>`, with each former `<svg …>geometry</svg>` reduced to `<svg …><use href="#gsi-N"/></svg>` (the outer `<svg>` keeps its attrs, so `currentColor`/stroke-width still theme the icon). It's an **inline** sprite, not an external `img/icons.svg` referenced via `<use href="file.svg#id">`, because browsers block external SVG `<use>` from `file://` (these pages open by double-click) — the sprite is *also* written to `img/icons-<page>.svg` as a reference asset. SVGs inside `<script>`/`<style>` were left untouched (quote-fragile). Icon geometry is thus defined once per page; **don't paste large repeated inline `<svg>` icons — add a `<symbol>` to the page's sprite and `<use>` it.** **04 used to inline the swoosh as a 131 KB base64 `var SW` blob stamped onto every `[data-sw]` image — that was removed; the `[data-sw]` loop now sets `el.src='img/swoosh.png'` (the same file, cached once), which cut ~131 KB off the page.** Keep assets in `img/`/`css/`/`js/`; don't re-embed base64 in the HTML.
- **Custom-select dropdown is a fixed-position portal**: `.cs-menu` is now `position:fixed` and positioned to the trigger on open (`positionMenu`, flipping above the trigger when there's no room below), so an ancestor's `overflow:hidden` can never clip it — that was why the bottom-of-card "Rows per page" list was hidden inside the card. It closes on any scroll and re-syncs its trigger label when the underlying `<select>.value` is set programmatically (`sel` listens to `change`→`sync`, which also fixes the clear-filters + page-jump labels).
- **Configure screen jurisdictions (v7 `s5`)**: the fixed EU/UK/Canada toggle list was replaced with the same country-search card used in add-retailer (`.csel-*` + `ALL_COUNTRIES`), but multi-select — picking a country adds a removable chip (`cfgAddJur`/`cfgRemoveJur`/`cfgRenderJur`, seeded with Ireland + United Kingdom). Its own ids (`cfg-jur-*`) keep it independent of the add-retailer picker's `csel-*` ids.
- **Pagination page-jump**: the paginated product tables (`pt-table*`, engine `ptRender`/`ptGoPage` inline in 01 v7 / 02 / 03) now include a **"Go to" page-jump `<select>`** (`ptJump(scope,val)`); `ptRender` rebuilds its options when the page count changes and syncs the themed label. (v6 is frozen and wasn't updated.)
- **Input micro-interactions**: `window.gsShake(el)` plays a quick shake + red border for invalid-submit feedback (`@keyframes gsShake`, `.fi.gs-shake`); an input with `data-rotate="a|b|c"` cycles its placeholder every ~2.8s while empty & unfocused with a fade (`initRotatePlaceholders`, `window.GSRotatePlaceholders`; demo on the v7 Retailers search). The animated focus ring already covers "focus transition". *Not yet done (larger/structural — ask before applying globally): floating labels and the typewriter effect.*

## The prototypes

| File | Persona |
|---|---|
| [01-Greenstreets_GreenSTreets_Admin_v7.html](01-Greenstreets_GreenSTreets_Admin_v7.html) | **Current** Greenstreets platform admin — glassmorphism redesign, all 16 screens, live theme customizer, and the data-grid toolkit on its listings |
| [01-Greenstreets_GreenSTreets_Admin_v6.html](01-Greenstreets_GreenSTreets_Admin_v6.html) | Frozen reference — the previous admin (superseded by v7; still links the shared theme, so it inherits shared fixes) |
| [02-Greenstreets_retailer_admin_v1.html](02-Greenstreets_retailer_admin_v1.html) | Retailer admin |
| [03-greenstreets_retailer_user_v1.html](03-greenstreets_retailer_user_v1.html) | Retailer user |
| [04-greenstreets_supplier_portal_v7.html](04-greenstreets_supplier_portal_v7.html) | Supplier portal (largest/most developed prototype) |

`03-greenstreets_retailer_user_v1/` is an empty directory alongside the same-named `.html` file — ignore it, it's stray. `old/` contains superseded versions of these prototypes (including `greenstreets_prototype_v3.html`/`v4.html`, earlier unified versions before the personas were split into separate files) plus one-off Python scripts (`clean.py`, `check.py`, `fix.py`) that were used to batch-edit an older prototype via regex; they aren't a repeatable pipeline and reference hardcoded absolute paths — don't treat them as tooling to run, just historical scratch scripts. `Img/` holds shared raster assets referenced by relative path from the admin redesigns: `swoosh.png` (accent-button texture overlay) and `BackgroundGreenStreets.jpg` (optional login background photo).
Ignore all folders containing 'old'
When asked to extend the GreenStreets Admin prototype, work in **v7** (or last v version name) — it's the actively maintained one. v6, v5 and v5_redesign are frozen references only (v6 still links the shared theme so it inherits cross-cutting fixes, but don't add new features there).

## Architecture of each prototype file

Every prototype is built the same way — a single `<html>` file with all CSS in one `<style>` block and all behavior in `<script>` blocks at the bottom. There's no framework, no modules, no build step: everything is vanilla JS/CSS/HTML in one document.

**Screens, not pages.** Each distinct view (login, list, detail, wizard step, etc.) is a `<div class="screen" id="sN">` (or `pN` for a step in a multi-step flow, e.g. add-retailer steps `p1`-`p4`). Only the screen with class `.on` is visible (`display:flex`); all others are `display:none`. Navigation between screens never triggers a page load — it's all client-side visibility toggling via a global `go(id)` function:

```js
function go(id){
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('on'));
  el = document.getElementById(id); if(el) el.classList.add('on');
  // also syncs the .pnb prototype-nav button highlight to match
}
```

Real UI elements (sidebar nav links, buttons, breadcrumbs, table rows) call `go('sN')` via inline `onclick` attributes to move between screens — this is the actual navigation UX being prototyped.

**Prototype nav bar (`.pnav` / `.pnb` buttons).** A sticky bar at the very top of the page (outside the real app UI) lists every screen as a button for quick jumping while reviewing the prototype — this is a reviewer/demo aid, not part of the product UI being designed. When adding a new screen, add both the `.screen` div and a corresponding `.pnb` button so it's reachable.

**Design tokens via CSS variables.** Each file defines its palette/spacing in a `:root{...}` block (e.g. `--gs` for the Greenstreets green accent, `--nv`/`--nv2`/`--nv3` for navy background shades, `--tw`/`--tw2`/`--tw3` for white-text opacity levels, `--rs`/`--rl` for border-radius scale). Reuse these variables rather than hardcoding new colors/radii when editing a screen — check the `:root` block at the top of the `<style>` section of the specific file you're editing, since values drift slightly between prototypes.

**No real data or backend.** All content (retailer names, stats, table rows) is static HTML baked into each screen, or occasionally a small inline JS array (e.g. a country picker's option list). Wizard steps and forms don't persist input between screens beyond simple JS-driven UI feedback (e.g. `selectCountry()` filling in a dependent field) — there's no state store.

## v6 admin prototype — extra architecture on top of the base pattern

v6 follows everything in "Architecture of each prototype file" above (screens, `go()`, `.pnav`/`.pnb`, CSS-variable design tokens), plus these additions built up over several redesign passes:

**Shared sidebar via JS injection, not copy-pasted markup.** The operator sidebar (logo, nav links, notifications, user footer) is identical across ~14 screens, so instead of duplicating that HTML per screen, each screen just declares an empty mount point:
```html
<div class="sidebar" data-active="s11"></div>
```
`sidebarHtml(activeId)` builds the full sidebar HTML (marking the nav item matching `activeId` as `.active`) and `mountSidebars()` fills every `[data-active]` sidebar on load. **When adding a new operator screen, use this mount-point pattern — do not hand-write sidebar markup.** The one exception is the Impersonate screen (`s6`), which has a genuinely different client-facing sidebar (different nav items, dashed logo border, "NORTHBRIDGE VIEW" tag) and is hardcoded inline instead of mounted.

**Header stat chips.** Page-level KPI numbers live in the `.pg-hdr-bar`, right-aligned next to the title, as compact `.stat-mini` chips inside a `.hdr-stat-row` — not as a separate `.stat-row` block under the header. This is the standard pattern for every screen with summary numbers (Retailers, Suppliers, Retailer detail, Impersonate, Products, Packagings, Supplier detail). Keep new "dashboard numbers" consistent with this rather than reintroducing full `.stat` cards in the body.

**Live theme customizer.** A "Theme" button in `.pnav` opens a panel (`#themePanel`) that edits CSS custom properties on `documentElement.style` in real time — color pickers for the brand palette, presets, and range sliders for imagery/effects (swoosh opacity, login background opacity/photo toggle, logo sizes, sidebar gradient colors/opacity/angle/spacing, card-glow speed/pause/opacity/colors, spotlight opacity/size/color). Everything it controls is a `:root` variable, so new appearance knobs should also be plain CSS variables consumed by the relevant selector, plus an entry in `EXTRA_CONTROLS` (range/toggle) or `THEME_GROUPS` (color) so they show up in the panel automatically.
- **Export/Import JSON**: `collectThemeState()`/`applyThemeState()` round-trip the whole theme (colors + extra + login-bg toggle + spotlight/sidebar hex colors) to/from a downloadable `.json` file. When adding a new themeable property, wire it into both functions or it won't survive export/import.
- **Login card effects**: the glowing contour (`.login-card::before`) sweeps diagonally (top-left → bottom-right → back) via a JS-generated `@keyframes` (`updateCardGlowAnimation()`) so the speed/pause controls can reshape the animation timing live — don't hand-edit the static fallback `@keyframes cardGlow` for behavior changes, edit the JS generator. The cursor-follow spotlight (`.zone-spotlight`) is a real element inside each login zone (not a card-level pseudo), positioned at `z-index:-1` so it glows through the frosted-glass zone background but stays behind the zone's real content (heading/inputs/buttons) — replicate this z-index/layering approach for any new glass-hover effects rather than a single card-wide overlay.

**Product/packaging interactivity ported from v5, restyled.** The paginated/sortable/filterable table engine (`ptInit`/`ptRender`/`ptSort`/etc.), the "Add packaging" library modal (`openPkgModal`/`PKG_LIBRARY`), and the country-selector dropdown (`ALL_COUNTRIES`/`selectCountry`) are the same logic as v5, just re-skinned with v6's classes (`.pill-*`, `.chev-btn`, glass `.grp` cards). Reuse these functions for any new data-table or picker UI rather than writing new ad-hoc JS.

## Working in this repo

- To preview a change, just open the `.html` file in a browser — opening `file://` works because the shared `css/`+`js/` files are referenced by relative path. For the browser-preview tooling in this environment, serve over HTTP instead (a `.Codex/launch.json` defines a `static` config = `python -m http.server 8777`; `preview_start` it, then navigate to `http://localhost:8777/<file>.html`) since relative asset paths + the tooling behave better over HTTP.
- **Cross-cutting look/behaviour changes go in the two shared files** (`css/greenstreets-theme.css`, `js/greenstreets-theme.js`), which all four pages link — not in a single page's inline `<style>`. After editing either, bump the `?v=N` query on the `<link>`/`<script>` in all four HTML files (grep `greenstreets-theme`) or a cached copy will hide your change. Page-specific components still live in that page's inline `<style>`.
- The preview tab runs unfocused/hidden, so `element.focus()` doesn't fire real `:focus`/focus events and `requestAnimationFrame` is throttled — you can't observe the focus-ring draw live there. Verify it structurally instead: dispatch a bubbling `focusin` to spawn the `.fs-ring`, freeze a frame by setting `--fs-a`, and check the served file contents / easing math.
- Every screen/nav-button/onclick edit is manual HTML — grep for the screen's `id="sN"` and its matching `.pnb` button (same index position in the nav bar list) when adding, removing, or reordering screens, since the two lists must stay in sync by position.
- Follow [SKILL.md](SKILL.md) (the `frontend-design` skill) for visual/UX design decisions — it governs tone, typography, layout, and copywriting choices for this project and is loaded automatically when doing design work here.
- Given the file sizes (some HTML files exceed 300KB and 1500+ lines), prefer targeted `Grep`/offset-based `Read` over reading a whole file at once, and prefer `Edit` for surgical changes over rewriting a file.
- In v6, watch your `<div>` balance around `<div class="sidebar" data-active="…"></div>` mount points — it's an empty element, easy to accidentally leave a stray closing `</div>` next to it when editing (this broke the Retailers screen's layout once: the stray tag closed `.app-body` early, so `.main` ended up as a sibling instead of a flex child and rendered full-width below the sidebar instead of beside it). After structural edits to a v6 screen, sanity-check with a quick browser reload rather than assuming the diff was safe.
- The preview browser tooling in this environment is occasionally flaky (`preview_screenshot` can hang/timeout even when the page is fine) — if screenshots stall, fall back to `preview_eval`/`preview_snapshot`/`preview_console_logs` to verify behavior structurally before retrying the screenshot.
